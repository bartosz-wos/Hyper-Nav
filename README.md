# Hyper-Nav
Fast file searcher using Suffix Automaton

## Features:
- **O(M) query time, for strings of length M**
- **Find files by any part of their path (Substring matching)**
- **Memory efficient**

## Build
```bash
make
./hypernav
